
services:
  container_name: convert
  build: .
  command: uvicorn main:app --reload --workers 1 --host 0.0.0.0 --port 8000 --log-level 'info'
  ports:
    - "0.0.0.0:8000:8000"
  volumes:
    - ./app/:/usr/src/app/
  restart: unless-stopped
