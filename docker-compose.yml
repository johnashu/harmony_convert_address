
services:
  convert:
    build: .
    command: gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    restart: unless-stopped
